# DRAFT registry entry for the `playtest` module — do NOT submit yet.
#
# This is the single list item to APPEND under the existing `modules:` key in
# GoMudEngine/GoMud-Modules/module-registry.yaml (the registry the module
# manager fetches: raw.githubusercontent.com/GoMudEngine/GoMud-Modules/
# refs/heads/master/module-registry.yaml). Schema (RegistryEntry):
# name, description, version, author, url, sha256.
#
# GATED — do not open the registry PR until BOTH are true:
#   1. The engine AI-port PR is merged into GoMudEngine/GoMud (done).
#   2. A v0.1.1 release of GoMudEngine/GoMud-Module-Playtest-Harness exists with
#      playtest.tar.gz attached (the `url` below) — and `sha256` matches THAT
#      released archive (verify by downloading it after publishing).
#
# sha256 below = `module package playtest` against the final v0.1.1 source.
# (v0.1.0 was superseded — it fabricated an account at boot; v0.1.1 keys off the
# AI-port connection and the agent creates its own character.)

- name: playtest
  description: >-
    AI playtest harness — per-round Playtest.Round GMCP beacons, structural
    safe-mode confinement, and death protection for any client on the AI port,
    for structured goal verification. No account provisioning: the AI agent logs
    in or creates a character via the normal new-player flow. Requires a GoMud
    with the AI-port primitives and the bundled gmcp module (for beacons). Pairs
    with the mudagent adapter + tester-personality framework at
    github.com/GoMudEngine/GoMud-Module-Playtest-Harness.
  version: 0.1.1
  author: pruuk
  url: https://github.com/GoMudEngine/GoMud-Module-Playtest-Harness/releases/download/v0.1.1/playtest.tar.gz
  sha256: ddd5c625f06b044ed802f17cf87f73e3ff7621b8e60362a2d47dbaabf9b4153a
