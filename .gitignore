# Compiled binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
/go-mud-server
/mudagent
/mudagent.exe
/dist/
/playtest.tar.gz

# Test / coverage output
*.test
*.out
coverage.*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Harness runtime scratch
.playtest/

# engine-profile.yaml and targets.yaml are committed working defaults (stock
# GoMud / localhost:55555); targets.yaml ships with blank credentials so no
# secrets are tracked. If you add credentials, don't commit them.
